#include<stdio.h>

int main()
{
    int a, h, t, u, b;

        for(h=0;h<=5;h++)
        {
            for(t=0;t<=9;t++)
            {
                 for(u=0;u<=9;u++)
                    {
                        a=(100*h)+(10*t)+(u);
                        b=(u*u*u)+(h*h*h)+(t*t*t);
                        if(a==b)
                        {
                             printf("\n %d", a);
                        }
                    }
            }
        }

        return 0;
}
